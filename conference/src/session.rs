/// A Session is a container of machines that form a single users environemnt. The session could process a single
/// mail item, or could process audio, video, chat for a web meeting. The point being that it represents a single
/// user. The Session lives within a Conference, which is a container of one or more sessions. The Conference manages
/// intra-session communications, while the session manages inter-session communications.
/// 

struct Session {
    
}
